module my

public type PType<O derives Any, P derives Any> {
    static MEMBER : Int = 1
}

public type PTypeMid<P> PType<P,Int>

public type PTypeSub PTypeMid<Float>

func main() Int {
    ##tst : PType.E

    mem := PTypeSub.MEMBER
    fop : PTypeSub.my#PType#P = 5
    foo1 : PTypeSub.P = 11.
    foo2 : PTypeSub.my#PType#O = 12.

    ##return fop
    ##return Int(foo1)
    return Int( foo1 + foo2 )
}
