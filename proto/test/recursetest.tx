
func puts(cstr : &[]UByte) {
    tx.c.puts(&cstr^[0])
}

func main() Int {
    recurse(10)
    recurse(10.)
    return 0
}

func recurse(x : Int) {
    puts(&c"recurse")
    if (x > 0) {
        recurse(x - 1)
    }
}

func recurse(x : Float) {
    puts(&c"float version")
}