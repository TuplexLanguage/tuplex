##func cstr(Int v)

func ret_array() &[]UByte {
    s : [4]UByte = c"foo"
    t : &[]UByte = &s
    ## u : [4]UByte = t  ## ERROR
    tx.c.puts(s)
    return t
    ##return c"foo"
}

func print(cstr : &[]UByte) {
    tx.c.puts( cstr^ )
}

func basic_array_syntax() Int {
    tx.c.puts( ret_array()^ )
    print( ret_array() )

    ##a : Array<Int,3>
    b : [10]~UByte;
    c := c"1234567890"
    b[0] = c[1]
    b[1] = c[0]
    b[2] = UByte(0)
    tx.c.puts(b)
    return c[1]-48
}

func main() Int {
    return basic_array_syntax()
}
